{% extends 'base.html.twig' %}

{% block title %}Edit Room Listing{% endblock %}

{% block content %}
<div class="max-w-3xl mx-auto py-10 px-6 bg-white shadow-lg rounded-xl mt-10">
    <h1 class="text-3xl font-bold text-gray-800 mb-6 text-center">Edit Room Listing</h1>

    {{ include('roomlisting/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="mt-6 flex justify-between items-center">
        <a href="{{ path('app_roomlisting_index') }}"
           class="bg-gray-200 hover:bg-gray-300 text-gray-800 font-semibold py-2 px-4 rounded-lg transition">
            ← Back to List
        </a>

        {# FIX: Changed to include the correct partial name '_delete_form.html.twig' #}
        {{ include('roomlisting/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}