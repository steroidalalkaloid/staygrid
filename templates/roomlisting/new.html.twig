{% extends 'base.html.twig' %}

{% block title %}Create New Room Listing{% endblock %}

{% block content %}
<div class="max-w-xl mx-auto py-12 px-4 sm:px-6 lg:px-8 pt-24">
    <div class="bg-white shadow overflow-hidden sm:rounded-lg p-6">
        <h1 class="text-3xl font-extrabold text-gray-900 mb-6">Create New Room Listing</h1>

        {{ include('roomlisting/_form.html.twig', {'button_label': 'Create'}) }}
        
        <div class="mt-6">
            <a href="{{ path('app_roomlisting_index') }}" 
               class="inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-yellow-500 transition">
                ← Back to list
            </a>
        </div>
    </div>
</div>
{% endblock %}